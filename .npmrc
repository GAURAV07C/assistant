# Network hardening for unstable connections (prevents frequent ETIMEDOUT during installs).
fetch-retries=6
fetch-retry-mintimeout=20000
fetch-retry-maxtimeout=120000
fetch-timeout=600000
maxsockets=5
prefer-offline=true

# Reduce extra network calls.
fund=false
audit=false
